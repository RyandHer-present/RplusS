# Local development only. Gitignored — never committed.
#
# Both values are public by design: they are compiled into the deployed bundle,
# which anyone can read. Row Level Security is what protects the data.
VITE_SUPABASE_URL=https://jpikfeprrgdrvcdwdhnr.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_kzFDT4ctiAZ-SEBo_pjYJQ_ZPF55nDC
